# 神农集团信息数字化平台 v1.0 - 完整开发文档

> 🏢 基于 www.ynsnjt.cn 内容定制的IT信息化平台

**文档版本：** v1.0.0  
**最后更新：** 2025年1月21日  
**维护团队：** 神农集团卓越运营中心

---

## 📑 目录

1. [项目概述](#项目概述)
2. [快速开始](#快速开始)
3. [项目架构](#项目架构)
4. [核心功能](#核心功能)
5. [开发指南](#开发指南)
6. [部署说明](#部署说明)
7. [更新日志](#更新日志)
8. [常见问题](#常见问题)
9. [技术支持](#技术支持)

---

## 项目概述

### 简介

神农集团信息数字化平台是为云南神农农业产业集团股份有限公司卓越运营中心提供的数字化解决方案。本项目整合了企业各类IT系统，提供统一的访问入口和管理界面，采用现代化的前端技术栈，提供良好的用户体验和响应式设计。

### 核心特性

- 🏠 **系统门户** - 集成所有IT系统的统一访问入口
- 🤖 **AI智能助手** - 提供IT系统相关问题的智能解答
- 📊 **系统管理** - 整合ERP、OA、CRM、SCM、BI、HR等核心系统
- 🔍 **搜索功能** - 快速搜索和定位所需系统
- ⭐ **收藏功能** - 收藏常用系统，提高使用效率
- 📈 **实时统计** - 显示在线用户、访问量等实时数据
- 📱 **响应式设计** - 完美支持桌面端、平板和移动端访问

### 技术栈

- **前端框架**: 原生JavaScript ES6+
- **构建工具**: Vite
- **样式技术**: CSS3 + CSS Variables
- **图标库**: Font Awesome
- **代码规范**: ESLint + Prettier
- **测试框架**: Vitest
- **浏览器支持**: 现代浏览器 (Chrome 80+, Firefox 75+, Safari 13+, Edge 80+)

---

## 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 8.0.0
- 现代浏览器 (Chrome, Firefox, Safari, Edge)

### 安装和运行

```bash
# 进入项目目录
cd shengnong-it-v1

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 访问地址

- 🏠 **开发服务器**: http://localhost:3001
- 🏢 **系统门户**: http://localhost:3001/#/
- 🤖 **AI助手**: http://localhost:3001/#/ai-assistant

---

## 项目架构

### 目录结构

```
shengnong-it-v1/
├── public/                 # 静态资源
├── src/                    # 源代码
│   ├── pages/             # 页面组件
│   │   ├── HomePage.js    # 首页
│   │   └── AIAssistantPage.js # AI助手页面
│   ├── services/          # 服务层
│   │   └── configService.js # 配置服务
│   ├── styles/            # 样式文件
│   │   ├── main.css       # 主样式
│   │   └── pages/         # 页面样式
│   │       ├── home.css   # 首页样式
│   │       └── ai-assistant.css # AI助手样式
│   ├── utils/             # 工具函数
│   │   ├── constants.js   # 常量定义
│   │   ├── eventBus.js    # 事件总线
│   │   └── helpers.js     # 辅助函数
│   ├── app.js             # 应用主类
│   ├── router.js          # 路由管理
│   └── main.js            # 应用入口
├── index.html             # HTML模板
├── vite.config.js         # Vite配置
├── package.json           # 项目配置
└── README.md              # 项目说明
```

### 架构设计

```
┌─────────────────────────────────────────┐
│         前端展示层 (Pages)               │
│      HomePage | AIAssistantPage         │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      业务逻辑层 (Services)               │
│      ConfigService | Router             │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      工具函数层 (Utils)                  │
│  EventBus | Helpers | Constants          │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      数据存储层 (Storage)                │
│  LocalStorage | SessionStorage           │
└─────────────────────────────────────────┘
```

---

## 核心功能

### 1. 系统门户 (HomePage)

**核心功能：**
- 🎯 统一入口 - 集成所有IT系统的统一访问入口
- 🔍 系统分类 - 按功能模块分类展示各类系统
- 🔎 搜索功能 - 快速搜索和定位所需系统
- ⭐ 收藏功能 - 收藏常用系统，提高使用效率
- 📈 实时统计 - 显示在线用户、访问量等实时数据

**集成系统清单：**

1. **ERP企业资源规划**
   - 财务管理
   - 人力资源
   - 采购管理
   - 销售管理
   - 一体化管理

2. **OA办公自动化**
   - 审批流程
   - 文档管理
   - 协同办公

3. **CRM客户关系管理**
   - 客户信息管理
   - 销售机会跟踪
   - 服务跟踪

4. **SCM供应链管理**
   - 供应商管理
   - 采购流程
   - 库存控制

5. **BI商业智能**
   - 数据分析
   - 报表展示
   - 决策支持

6. **HR人力资源系统**
   - 员工管理
   - 薪酬计算
   - 绩效考核

### 2. AI智能助手 (AIAssistantPage)

**核心功能：**
- 💬 **智能咨询** - 提供IT系统相关问题的智能解答
- 🛠️ **技术支持** - 协助解决系统使用中的技术问题
- 📊 **系统监控** - 实时监控各系统运行状态
- 📈 **数据分析** - 分析系统使用情况和性能数据

**技术特性：**
- 🔄 实时对话
- 🧠 上下文理解
- 📝 消息管理
- ⚙️ 配置管理

---

## 开发指南

### 代码规范

项目使用 ESLint 和 Prettier 进行代码规范检查：

```bash
# 检查代码规范
npm run lint

# 自动修复代码规范问题
npm run lint:fix

# 格式化代码
npm run format
```

### 测试

```bash
# 运行测试
npm run test

# 运行测试并生成覆盖率报告
npm run test:coverage

# 运行测试UI界面
npm run test:ui
```

### 添加新系统

1. 在 `src/utils/constants.js` 中的 `IT_SYSTEMS` 数组添加新系统配置：

```javascript
{
  id: 'new-system',
  name: '新系统名称',
  description: '系统描述',
  icon: 'fas fa-icon-name',
  url: 'https://system-url.com',
  category: 'category-id',
  color: '#color-code'
}
```

2. 如需新分类，在 `SYSTEM_CATEGORIES` 中添加分类定义：

```javascript
{
  id: 'new-category',
  name: '新分类名称',
  icon: 'fas fa-icon-name'
}
```

3. 系统会自动在首页显示

### 添加新页面

1. 在 `src/pages/` 创建页面组件
2. 在 `src/router.js` 注册路由
3. 在 `src/app.js` 配置路由映射
4. 添加对应的样式文件到 `src/styles/pages/`

### 自定义样式

- **全局样式变量**: 定义在 `src/styles/main.css` 的 `:root` 中
- **页面特定样式**: 放在 `src/styles/pages/` 目录下
- **组件样式**: 直接写在对应的页面组件中

**CSS变量示例：**
```css
:root {
  --primary-color: #1e40af;
  --secondary-color: #3b82f6;
  --background-color: #f8fafc;
  --text-color: #1e293b;
}
```

### 配置管理

```javascript
import { ConfigService } from './services/configService.js'

const configService = new ConfigService()
await configService.loadConfig()

// 获取配置
const config = configService.getConfig('key')

// 设置配置
await configService.setConfig('key', value)
```

### 事件通信

```javascript
import { EventBus } from './utils/eventBus.js'

// 监听事件
EventBus.on('event:name', (data) => {
  console.log('事件触发:', data)
})

// 触发事件
EventBus.emit('event:name', { key: 'value' })

// 取消监听
EventBus.off('event:name', handler)
```

---

## 部署说明

### 构建生产版本

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

构建完成后，将 `dist` 目录部署到任何静态文件服务器即可。

### Nginx配置示例

```nginx
server {
    listen 80;
    server_name www.ynsnjt.cn;
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

### Docker部署

```dockerfile
FROM nginx:alpine

COPY dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

---

## 更新日志

### v1.0.0 (2025-01-01)

**初始版本发布：**
- 🎉 项目初始化
- ✨ 实现系统门户功能
- ✨ 集成AI智能助手
- ✨ 支持响应式设计
- ✨ 基于www.ynsnjt.cn内容定制
- ✨ 集成ERP、OA、CRM、SCM、BI、HR等核心系统
- ✨ 实现搜索和收藏功能
- ✨ 添加实时统计功能

---

## 常见问题

### Q1: 如何添加新的IT系统？
**A:** 在 `src/utils/constants.js` 的 `IT_SYSTEMS` 数组中添加新系统配置即可。

### Q2: 如何修改系统主题颜色？
**A:** 修改 `src/styles/main.css` 中的 CSS 变量，如 `--primary-color`。

### Q3: 如何配置开发服务器端口？
**A:** 在 `vite.config.js` 中修改 `server.port` 配置。

### Q4: 如何启用HTTPS？
**A:** 在 `vite.config.js` 中配置 `server.https` 选项。

### Q5: 构建后的文件在哪里？
**A:** 构建后的文件在 `dist` 目录中。

### Q6: 如何清除缓存？
**A:** 删除 `node_modules` 和 `package-lock.json`，然后重新运行 `npm install`。

---

## 技术支持

### 联系方式

- **公司**: 云南神农农业产业集团股份有限公司
- **部门**: 卓越运营中心
- **网站**: www.ynsnjt.cn
- **技术支持**: 400-626-8888
- **邮箱**: it@ynsnjt.com
- **工作时间**: 周一至周五 9:00-18:00

### 相关链接

- **官方网站**: https://www.ynsnjt.cn
- **技术文档**: 查看 `docs/` 目录
- **问题反馈**: 联系技术支持团队

---

## 许可证

MIT License

---

<div align="center">

**🏢 神农集团信息数字化平台 v1.0 - 数字化转型的基石 🏢**

Made with ❤️ by 神农集团卓越运营中心

</div>
